{
  "_note": "Run: .venvs/32_nepa/bin/python bin/mutate.py --spec mutations/32_nepa-step1-device.json --python /home/yoshihiro_fukuhara_caddi_jp/10YearVisualSSL/.venvs/32_nepa/bin/python (an interpreter with this method's lock and a CUDA device). Proves the device guard is not vacuous. See docs/GPU.md section 4.",
  "targets": [
    {
      "label": "cpu is not honoured -- the requested cpu returns a cuda device",
      "file": "methods/32_nepa/train_step1_nepa.py",
      "old": "    if spec == \"cpu\":\n        return torch.device(\"cpu\")",
      "new": "    if spec == \"cpu\":\n        return torch.device(\"cuda\")",
      "tests": [
        "tests.test_method_32_nepa.TestTheDeviceIsResolved"
      ]
    },
    {
      "label": "cuda without a gpu is no longer refused, it falls back silently",
      "file": "methods/32_nepa/train_step1_nepa.py",
      "old": "        if not torch.cuda.is_available():",
      "new": "        if False:",
      "tests": [
        "tests.test_method_32_nepa.TestTheDeviceIsResolved"
      ]
    },
    {
      "label": "the images ignore the resolved device and go to the GPU regardless",
      "file": "methods/32_nepa/train_step1_nepa.py",
      "old": "            images = images.to(device, non_blocking=True)",
      "new": "            images = images.cuda(0, non_blocking=True)",
      "tests": [
        "tests.test_method_32_nepa.TestAStep1Smoke.test_it_completes_and_satisfies_the_contract"
      ]
    }
  ],
  "_result": {
    "measured_on": "2026-08-10",
    "measured_with": ".venvs/32_nepa (cu130) on an NVIDIA Tesla T4",
    "killed": 3,
    "total": 3,
    "equivalent": []
  }
}
