{
  "_note": "Run: .venvs/14_simclrv1/bin/python bin/mutate.py --spec mutations/14_simclrv1-step1-device.json --python /home/yoshihiro_fukuhara_caddi_jp/10YearVisualSSL/.venvs/14_simclrv1/bin/python (an interpreter with this method's lock and a CUDA device). Proves the device guard is not vacuous. See docs/GPU.md section 4.",
  "targets": [
    {
      "label": "cpu is not honoured -- the requested cpu returns a cuda device",
      "file": "methods/14_simclrv1/train_step1_simclr.py",
      "old": "    if spec == \"cpu\":\n        return torch.device(\"cpu\")",
      "new": "    if spec == \"cpu\":\n        return torch.device(\"cuda\")",
      "tests": [
        "tests.test_method_14_simclrv1.TestTheDeviceIsResolved"
      ]
    },
    {
      "label": "cuda without a gpu is no longer refused, it falls back silently",
      "file": "methods/14_simclrv1/train_step1_simclr.py",
      "old": "        if not torch.cuda.is_available():",
      "new": "        if False:",
      "tests": [
        "tests.test_method_14_simclrv1.TestTheDeviceIsResolved"
      ]
    },
    {
      "label": "the batch ignores the resolved device and goes to the GPU regardless",
      "file": "methods/14_simclrv1/train_step1_simclr.py",
      "old": "            view1 = view1.to(device, non_blocking=True)",
      "new": "            view1 = view1.cuda(0, non_blocking=True)",
      "tests": [
        "tests.test_method_14_simclrv1.TestAStep1Smoke.test_it_completes_and_satisfies_the_contract"
      ]
    }
  ],
  "_result": {
    "measured_on": "2026-08-09",
    "measured_with": ".venvs/14_simclrv1 (cu130) on an NVIDIA Tesla T4",
    "killed": 3,
    "total": 3,
    "equivalent": []
  }
}
