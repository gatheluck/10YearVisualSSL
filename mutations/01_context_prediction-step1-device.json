{
  "_note": "Run: .venvs/1_context_prediction/bin/python bin/mutate.py --spec mutations/01_context_prediction-step1-device.json --python /home/yoshihiro_fukuhara_caddi_jp/10YearVisualSSL/.venvs/1_context_prediction/bin/python (an interpreter with this method's lock and a CUDA device). Proves the device guard is not vacuous. See docs/GPU.md section 4.",
  "targets": [
    {
      "label": "cpu is not honoured -- the requested cpu returns a cuda device",
      "file": "methods/01_context_prediction/train_step1_alexnet_official.py",
      "old": "    if requested == \"cpu\":\n        return torch.device(\"cpu\")",
      "new": "    if requested == \"cpu\":\n        return torch.device(\"cuda\")",
      "tests": [
        "tests.test_method_01_context_prediction.TestASmokeRun.test_cpu_is_honoured_even_where_cuda_exists"
      ]
    },
    {
      "label": "cuda without a gpu is no longer refused, it falls back silently",
      "file": "methods/01_context_prediction/train_step1_alexnet_official.py",
      "old": "        if not torch.cuda.is_available():",
      "new": "        if False:",
      "tests": [
        "tests.test_method_01_context_prediction.TestASmokeRun.test_asking_for_cuda_without_cuda_is_refused"
      ]
    },
    {
      "label": "the batch ignores the resolved device and goes to the GPU regardless",
      "file": "methods/01_context_prediction/train_step1_alexnet_official.py",
      "old": "            first = first.to(device, non_blocking=True)",
      "new": "            first = first.cuda(0, non_blocking=True)",
      "tests": [
        "tests.test_method_01_context_prediction.TestASmokeRun.test_it_completes_and_satisfies_the_contract"
      ]
    }
  ],
  "_result": {
    "measured_on": "2026-08-11",
    "measured_with": ".venvs/1_context_prediction (cu130) on an NVIDIA Tesla T4",
    "killed": 3,
    "total": 3,
    "equivalent": []
  }
}
