{
  "_note": "Run: .venvs/15_mocov2/bin/python bin/mutate.py --spec mutations/15_mocov2-pretrain-device.json --python /home/yoshihiro_fukuhara_caddi_jp/10YearVisualSSL/.venvs/15_mocov2/bin/python (an interpreter with this method's lock and a CUDA device). Proves the device guard is not vacuous. See docs/GPU.md section 4.",
  "targets": [
    {
      "label": "cpu is not honoured -- the requested cpu returns a cuda device",
      "file": "methods/15_mocov2/train_pretrain_mocov2.py",
      "old": "    if spec == \"cpu\":\n        return torch.device(\"cpu\")",
      "new": "    if spec == \"cpu\":\n        return torch.device(\"cuda\")",
      "tests": [
        "tests.test_method_15_mocov2.TestTheDeviceIsResolved"
      ]
    },
    {
      "label": "cuda without a gpu is no longer refused, it falls back silently",
      "file": "methods/15_mocov2/train_pretrain_mocov2.py",
      "old": "        if not torch.cuda.is_available():",
      "new": "        if False:",
      "tests": [
        "tests.test_method_15_mocov2.TestTheDeviceIsResolved"
      ]
    },
    {
      "label": "the batch ignores the resolved device and goes to the GPU regardless",
      "file": "methods/15_mocov2/train_pretrain_mocov2.py",
      "old": "            im_q = im_q.to(device, non_blocking=True)",
      "new": "            im_q = im_q.cuda(0, non_blocking=True)",
      "tests": [
        "tests.test_method_15_mocov2.TestAStep1Smoke.test_it_completes_and_satisfies_the_contract"
      ]
    }
  ],
  "_result": {
    "measured_on": "2026-08-09",
    "measured_with": ".venvs/15_mocov2 (cu130) on an NVIDIA Tesla T4",
    "killed": 3,
    "total": 3,
    "equivalent": []
  }
}
