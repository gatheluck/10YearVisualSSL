{
  "_note": "Run: .venvs/25_mae/bin/python bin/mutate.py --spec mutations/25_mae-step1-device.json --python /home/yoshihiro_fukuhara_caddi_jp/10YearVisualSSL/.venvs/25_mae/bin/python (an interpreter with this method's lock and a CUDA device). Proves the device guard is not vacuous: honouring device: cpu on a GPU machine, and refusing device: cuda on a GPU-less one. See docs/GPU.md section 4.",
  "targets": [
    {
      "label": "cpu is not honoured -- the requested cpu returns a cuda device",
      "file": "methods/25_mae/train_step1_mae.py",
      "old": "    if spec == \"cpu\":\n        return torch.device(\"cpu\")",
      "new": "    if spec == \"cpu\":\n        return torch.device(\"cuda\")",
      "tests": [
        "tests.test_method_25_mae.TestTheDeviceIsResolved"
      ]
    },
    {
      "label": "cuda without a gpu is no longer refused, it falls back silently",
      "file": "methods/25_mae/train_step1_mae.py",
      "old": "        if not torch.cuda.is_available():",
      "new": "        if False:",
      "tests": [
        "tests.test_method_25_mae.TestTheDeviceIsResolved"
      ]
    },
    {
      "label": "the batch ignores the resolved device and goes to the GPU regardless",
      "file": "methods/25_mae/train_step1_mae.py",
      "old": "            imgs = imgs.to(device, non_blocking=True)",
      "new": "            imgs = imgs.cuda(0, non_blocking=True)",
      "tests": [
        "tests.test_method_25_mae.TestAStep1Smoke.test_it_completes_and_satisfies_the_contract"
      ]
    }
  ],
  "_result": {
    "measured_on": "2026-08-05",
    "measured_with": ".venvs/25_mae (cu130 lock) on an NVIDIA Tesla T4",
    "killed": 3,
    "total": 3,
    "equivalent": []
  }
}
