{
  "_note": "Run: .venvs/23_dino/bin/python bin/mutate.py --spec mutations/23_dino-pretrain-device.json --python /home/yoshihiro_fukuhara_caddi_jp/10YearVisualSSL/.venvs/23_dino/bin/python (an interpreter with this method's lock and a CUDA device). Proves the device guard is not vacuous. See docs/GPU.md section 4.",
  "targets": [
    {
      "label": "cpu is not honoured -- the requested cpu returns a cuda device",
      "file": "methods/23_dino/train_pretrain_dino.py",
      "old": "    if spec == \"cpu\":\n        return torch.device(\"cpu\")",
      "new": "    if spec == \"cpu\":\n        return torch.device(\"cuda\")",
      "tests": [
        "tests.test_method_23_dino.TestTheDeviceIsResolved"
      ]
    },
    {
      "label": "cuda without a gpu is no longer refused, it falls back silently",
      "file": "methods/23_dino/train_pretrain_dino.py",
      "old": "        if not torch.cuda.is_available():",
      "new": "        if False:",
      "tests": [
        "tests.test_method_23_dino.TestTheDeviceIsResolved"
      ]
    },
    {
      "label": "the crops ignore the resolved device and go to the GPU regardless",
      "file": "methods/23_dino/train_pretrain_dino.py",
      "old": "            crops = [c.to(device, non_blocking=True) for c in crops]",
      "new": "            crops = [c.cuda(0, non_blocking=True) for c in crops]",
      "tests": [
        "tests.test_method_23_dino.TestAStep1Smoke.test_it_completes_and_satisfies_the_contract"
      ]
    }
  ],
  "_result": {
    "measured_on": "2026-08-09",
    "measured_with": ".venvs/23_dino (cu130) on an NVIDIA Tesla T4",
    "killed": 3,
    "total": 3,
    "equivalent": []
  }
}
