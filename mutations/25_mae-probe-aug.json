{
  "_note": "Run under the method venv (the train-branch test needs torchvision): /home/yoshihiro_fukuhara_caddi_jp/10YearVisualSSL/.venvs/25_mae/bin/python bin/mutate.py --spec mutations/25_mae-probe-aug.json --python /home/yoshihiro_fukuhara_caddi_jp/10YearVisualSSL/.venvs/25_mae/bin/python. Proves the BASIC5 rule `aug` wiring in this feature-cache evaluator is not vacuous: the train split must route through probe_transforms (RandomResizedCrop + HorizontalFlip), and run() must build the train split with train=True. The rule itself is proven in mutations/basic5-aug.json; these two targets prove this method wires it.",
  "targets": [
    {
      "label": "the train branch is never taken, so the probe's train split falls back to the deterministic eval transform and BASIC5 rule aug (RandomResizedCrop + HorizontalFlip) is not applied",
      "file": "methods/25_mae/evaluate_linear_mae.py",
      "old": "    if train:",
      "new": "    if False:",
      "tests": [
        "tests.test_method_25_mae.TestTheProbeTrainAugmentation.test_the_train_loader_applies_rrc_and_hflip"
      ]
    },
    {
      "label": "run() builds the train split without augmentation (train=False), so the cached features come from the deterministic eval transform",
      "file": "methods/25_mae/evaluate_linear_mae.py",
      "old": "                                     train=True)",
      "new": "                                     train=False)",
      "tests": [
        "tests.test_method_25_mae.TestTheProbeTrainAugmentation.test_run_builds_the_train_loader_with_augmentation"
      ]
    }
  ],
  "_result": {
    "measured_on": "2026-09-06",
    "measured_with": ".venvs/25_mae (cu130) on this host (CPU tests)",
    "killed": 2,
    "total": 2,
    "equivalent": []
  }
}
