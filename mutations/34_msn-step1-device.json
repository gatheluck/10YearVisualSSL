{
  "_note": "Run: .venvs/34_msn/bin/python bin/mutate.py --spec mutations/34_msn-step1-device.json --python /home/yoshihiro_fukuhara_caddi_jp/10YearVisualSSL/.venvs/34_msn/bin/python (an interpreter with this method's lock, the msn submodule checked out, and a CUDA device). Proves the device guard is not vacuous. See docs/GPU.md section 4.",
  "targets": [
    {
      "label": "cpu is not honoured -- the requested cpu returns a cuda device",
      "file": "methods/34_msn/train_step1_msn.py",
      "old": "    if spec == \"cpu\":\n        return torch.device(\"cpu\")",
      "new": "    if spec == \"cpu\":\n        return torch.device(\"cuda\")",
      "tests": [
        "tests.test_method_34_msn.TestTheDeviceIsResolved"
      ]
    },
    {
      "label": "cuda without a gpu is no longer refused, it falls back silently",
      "file": "methods/34_msn/train_step1_msn.py",
      "old": "        if not torch.cuda.is_available():",
      "new": "        if False:",
      "tests": [
        "tests.test_method_34_msn.TestTheDeviceIsResolved"
      ]
    },
    {
      "label": "the batch ignores the resolved device and goes to the GPU regardless",
      "file": "methods/34_msn/train_step1_msn.py",
      "old": "            imgs = [u.to(device, non_blocking=True) for u in udata]",
      "new": "            imgs = [u.cuda(0, non_blocking=True) for u in udata]",
      "tests": [
        "tests.test_method_34_msn.TestAStep1Smoke.test_it_completes_and_satisfies_the_contract"
      ]
    }
  ],
  "_result": {
    "measured_on": "2026-08-11",
    "measured_with": ".venvs/34_msn (cu130, third_party/msn checked out) on an NVIDIA Tesla T4",
    "killed": 3,
    "total": 3,
    "equivalent": []
  }
}
