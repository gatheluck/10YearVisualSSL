# The downstream evaluation environment's direct dependencies (floors only).
#
# One environment for all four cross-method downstream tasks (docs/DOWNSTREAM.md):
# ADE20K segmentation, COCO detection, NYUv2 depth and SSv2 video. The exact
# hashed closure is in downstream/requirements.lock.txt, installed by the CI
# `downstream` job so the task smokes run there and do not merely skip.
#
# torch/torchvision/timm are the fleet versions (a downstream task freezes a
# method's ViT backbone). The task-specific deps are the ones no method carries:
# pycocotools (COCO mAP), h5py (NYUv2 .mat), av / PyAV (SSv2 video decode).
torch>=2.1.0
torchvision>=0.16.0
timm>=0.9.0
numpy>=1.24.0
PyYAML>=6.0
Pillow>=9.0
pycocotools>=2.0.7
h5py>=3.0
av>=10.0
