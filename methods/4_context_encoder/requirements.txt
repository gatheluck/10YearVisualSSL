# Context Encoder step 1 + linear evaluation: the packages the ported files
# import. Floors only; the exact hashed closure is in requirements.lock.txt
# (CPU) and requirements.lock.cu130.txt (CUDA 13.0).
#
# The capture's requirements.txt also listed timm, opencv-python, matplotlib,
# scikit-learn, tensorboard, tqdm, numpy and Pillow; none is imported directly
# by the step-1 port (timm was only for the excluded ViT/step 2; Pillow comes
# in transitively via torchvision's ImageFolder). PyYAML is read by the trainer
# to load a YAML config.
torch>=2.0.0
torchvision>=0.15.0
PyYAML>=6.0
