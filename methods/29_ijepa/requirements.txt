# What the 29_ijepa port's own files import, and nothing else. Checked in both
# directions by tests/test_method_requirements.py. Floors only; the exact hashed
# closure is in requirements.lock.txt (CPU) and requirements.lock.cu130.txt.
#
# A self-contained re-implementation (the lab's own I-JEPA code, no submodule):
# the ViT context/target encoder and the narrow predictor (its own
# vision_transformer.py, NOT timm), the multi-block mask collator, the cosine
# schedulers, the dataset, the trainer and the probe are all here. torch is
# imported directly; numpy backs the schedulers; torchvision provides the
# ImageFolder and transforms; PyYAML is read to load a YAML config on the
# stand-alone path. The ViT step 2 is excluded, as in every port; I-JEPA's ViT is
# self-contained, so no timm, and TensorBoard is dropped.
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
PyYAML>=6.0
