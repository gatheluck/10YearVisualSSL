# What the 06_rotation_prediction port's own files import, and nothing else.
# Checked in both directions by tests/test_method_requirements.py. Floors only;
# the exact hashed closure is in requirements.lock.txt (CPU) and
# requirements.lock.cu130.txt.
#
# A self-contained re-implementation (the lab's own rotation code, no submodule):
# the model, the rotation dataset, the trainer and the probe are all here. torch
# is imported directly; numpy seeds the run; torchvision provides the ImageFolder
# and transforms (which read images through Pillow, a transitive dependency this
# port never imports itself); PyYAML is read to load a YAML config on the
# stand-alone path.
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
PyYAML>=6.0
# The Step-2 unified backbone (configs/pretrain_vit.yaml) is timm's
# VisionTransformer, built from scratch (models/vit_rotation.py). The native
# AlexNet pretrain does not import timm; it is only pulled in on the arch: vit
# path.
timm>=0.9.0
