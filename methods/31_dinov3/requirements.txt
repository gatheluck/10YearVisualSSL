# What the 31_dinov3 port's own files import, and nothing else. Checked in both
# directions by tests/test_method_requirements.py. Floors only; the exact hashed
# closure is in requirements.lock.txt (CPU) and requirements.lock.cu130.txt.
#
# A self-contained re-implementation (the lab's own DINOv3, no submodule): the
# ViT (register tokens + axial RoPE), the DINO/iBOT/KoLeo/Gram losses, the
# multi-crop dataset, the trainer and the probe are all here. torch is imported
# directly; torchvision provides the ImageFolder and transforms; numpy backs the
# seeding; PyYAML is read to load a YAML config on the stand-alone path. Only the
# from-scratch step 2 (the unified SSL comparison on ImageNet) is ported; the
# capture's step 1 (loading the HF-gated official DINOv3 weights, which needs
# transformers + huggingface_hub) is EXCLUDED, so neither is a dependency here.
# No timm; TensorBoard and tqdm are dropped, and the DDP paths are made inert.
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
PyYAML>=6.0
