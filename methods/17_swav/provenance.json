{
  "captured_at": "2026-08-01",
  "captured_from": "gatheluck/10YearVisualSSLCapturePrivate, branch snapshots",
  "captured_path": "methods/17_swav",
  "captured_sha256": {
    "models/resnet_swav.py": "555fe5eb92f0f9fcd0c79d5426133b9fb8cc631cbe26625a8809d11529a07cd4",
    "distributed_sinkhorn.py": "1e6696fb4677b9485eb7c9ac11e0e3f7378a07b6177728b1a904bf0d20dc8f5e",
    "checkpoint_utils.py": "284f9a37d4f90b6bbcd69c7b259400ab5b9746a17f7415201269b510e1beef5a"
  },
  "note": "Only captured_sha256 came across untouched. Step 2 (the ViT) was not brought across: it has no official-style variant in the capture. The checkpoint directory in the captured config was an absolute path on the cluster and is not shipped. The linear evaluation was added in a later port and reuses the captured evaluate_linear.py (recorded below).",
  "rewritten_during_the_port": {
    "__init__.py": "empty in the capture; kept empty",
    "evaluate_linear.py": "captured sha256 01b266c9b3eb35ba833d26edf8c2d6376542a5cf05b2f7b7520155c3b03b1f1a. Added in the linear-evaluation port. resolve_device()/make_deterministic() imported from the trainer -- one implementation per method -- and the unconditional torch.device('cuda:gpu')/torch.cuda.set_device replaced; the captured evaluation could not start without a GPU. main() split into build_parser() and run(args, encoder, in_dim), which returns its metrics; the captured version wrote results.json and returned nothing. run() accepts an encoder built by the caller, because the contract's artifact is encoder.pt (the backbone alone) while the captured code rebuilds the whole model from a training checkpoint. model_type='vit' is refused by name; the branch that imports build_vit_swav (step 2, absent) is unreachable.",
    "configs/linear_eval.yaml": "new: the linear-evaluation recipe as a contract config; model_type resnet is fixed by the adapter.",
    "models/__init__.py": "advertised the step 2 ViT through a lazy __getattr__. The module was not brought across, so the package was promising a name it could not import.",
    "data/__init__.py": "re-exported the step 2 loader, which was not brought across",
    "train_step1_resnet.py": "resolve_device() and make_deterministic() added, and the unconditional .cuda(local_rank) on the model and on each crop replaced by .to(device); the captured trainer could not start without a GPU. main() split into build_parser() and run(args, config), which returns the epoch loss and the epoch count; the captured version computed them and discarded them.",
    "data/multi_crop.py": "captured sha256 c-- taken before the change. get_swav_dataloader built a DistributedSampler unconditionally, which needs an initialised process group, so this method could not run on one process at all -- on a GPU or otherwise. The sampler is now conditional, the same way every other ported method's loader already had it, and the trainer passes its own `distributed` flag through. Unlike the change the port before this one made and reverted, this one is for a failure that was observed: the run raised 'Default process group has not been initialized'."
  }
}
