# What the 18_sela port's own files import, and nothing else. Checked in both
# directions by tests/test_method_requirements.py. Floors only; the exact hashed
# closure is in requirements.lock.txt (CPU) and requirements.lock.cu130.txt.
#
# A self-contained re-implementation (the lab's own SeLa code, no submodule): the
# ResNet backbone + prototype heads, the torch Sinkhorn-Knopp optimal transport,
# the indexed dataset, the trainer and the probe are all here. torch is imported
# directly; numpy seeds the run and builds the balanced-label init + LR schedule;
# torchvision provides ResNet-50, the ImageFolder and transforms; PyYAML is read
# to load a YAML config on the stand-alone path. timm supplies the ViT-B/16 for
# the unified Step-2 path (arch: vit), imported lazily so the native ResNet path
# does not need it.
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
PyYAML>=6.0
timm>=0.9.0
