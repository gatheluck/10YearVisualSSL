{
  "captured_at": "2026-07-30",
  "captured_from": "gatheluck/10YearVisualSSLCapturePrivate, branch snapshots",
  "captured_path": "methods/02_vae",
  "captured_sha256": {
    "data/vae_dataset.py": "803a2e1a14474ab2c300d25cf4a2f65d254ba30006aa398e7c0618f36e42a0ef",
    "models/vae_cnn.py": "c8053143e3fd1c5c7b9103d5ea70fe1cf1379873c228d3d806b570fbf12598ae"
  },
  "note": "Only captured_sha256 came across untouched. The output path in the captured config was an absolute path on the cluster and is not shipped; the adapter puts it under --out.",
  "rewritten_during_the_port": {
    "__init__.py": "re-exported VAE_ViT (step 2), which was not brought across",
    "data/__init__.py": "same",
    "models/__init__.py": "same",
    "train_pretrain_cnn.py": "main() split into parse_args() and run(args, config); make_deterministic() called; the config may be passed in rather than read from a file, so the adapter can redirect the output path"
  },
  "step1_native_artifact": {
    "sha256": "c3b736c76a6c77a109d7151b03505da34343748a6ae85d56ac3510500d88cd00",
    "availability": "user_supplied",
    "acquisition": "Request the lab-trained Step-1 checkpoint matching this SHA-256 from the authors. No public download URL for these exact bytes is verified. Keep encoder.pt and export.json together.",
    "checkpoint_epoch_stored": 300,
    "state_key": "model_state_dict",
    "strip_prefix": "",
    "feature_options": {
      "config_overrides": {
        "train": {
          "latent_dim": 50,
          "img_size": 224
        }
      },
      "resize_short_side": 256
    },
    "selection_evidence": "The recorded native Step-1 evaluation references this checkpoint. Stored epoch and configuration were inspected with weights_only=True; SHA-256 was measured from the actual file. Feature parity must be verified against the native evaluation before use."
  }
}
