{
  "captured_at": "2026-08-02",
  "captured_from": "gatheluck/10YearVisualSSLCapturePrivate, branch snapshots",
  "captured_path": "methods/27_ibot",
  "captured_sha256": {
    "models/ibot.py": "13d6290e575b2d417facdfd8f3797eacab2dd6cbffc8e3b4b5f9da53de18d226",
    "models/vision_transformer.py": "1feaac57614f190835c7111eadd5f17118dcefb9ad44191ee66629553b14decb",
    "data/multicrop.py": "1014a889d86f7d4f727e73c97693726727d619d848549908149544f532dc4222",
    "models/__init__.py": "aecbea3224e543b4628a74113106044b87d4d30b2e05564d82aa7f998128745d",
    "data/__init__.py": "a8ad879a5e3f2d045d32ba6b1c028e37ca5d15d4155fd9bb30f91a84bde75899",
    "__init__.py": "78c4fdcef1ee5a5a0ea1c13374f235540549bfa40122a581f3253ce45bbf8a43"
  },
  "note": "Only captured_sha256 came across untouched, and their digests are pinned by tests/test_method_27_ibot.py. The model (student/teacher ViT, DINOHead, iBOTLoss) and the multi-crop loader are device-agnostic and needed no change; the loader already builds a DistributedSampler only when distributed, so it runs on one process unchanged. Step 2 (ViT-B) has no place in this port: train_step2_vit.py, configs/step2_vit_b.yaml, the step 2 shell script and tests/test_step2_protocol.py were not brought across (CONTRACT stages are pretrain and linear_eval). The checkpoint directory in the captured config was an absolute path on the cluster and is not shipped; the adapter puts scratch under --out/work. requirements.txt was rewritten to declare only what the ported files import (timm, tqdm and einops were listed in the capture but never imported -- the ViT is implemented here, not from timm).",
  "rewritten_during_the_port": {
    "train_pretrain.py": "captured sha256 9358b1e05e47e1ab0f5c92ea0afd2d5122ba6359c95354ab84a225ca3c339a78. resolve_device() and make_deterministic() added; the unconditional .cuda(local_rank) on the model, the loss and every crop and mask replaced by .to(device) -- the captured trainer could not start without a GPU. main() split into build_parser() and run(args, config), which returns the epoch loss and its cls/patch components and the epoch count; the captured version computed them and discarded them. The backbone is chosen from model.arch (vit_small only; vit_base is step 2) rather than hard-coded, so the resolved config says which architecture ran.",
    "evaluate_linear.py": "captured sha256 c3778c2be63c697aee01f5209742053405d5a8ab68ddada086fa32932b3cd126. resolve_device()/make_deterministic() imported from the trainer -- one implementation per method -- and the unconditional torch.device('cuda:gpu') and torch.cuda.set_device replaced; the captured evaluation could not start without a GPU. main() split into build_parser() and run(args, encoder, in_dim), which returns its metrics; the captured version wrote results.json and returned nothing. run() accepts an encoder built by the caller, because the contract's artifact is encoder.pt (the teacher backbone alone) while the captured code rebuilds the whole iBOT model from a training checkpoint with strict=True and takes its teacher -- teaching this file a second way to recognise a file would be a second place that knows how an encoder is loaded. The checkpoint health gate is reached only on the stand-alone path, where a checkpoint is actually read.",
    "adapter/__init__.py": "new: translates the resolved config into the nested shape the trainer reads, refuses a config naming an output location, declares every setting the trainer reads (no silent defaults), and extracts encoder.pt as the teacher ViT (ENCODER_PREFIX 'teacher.'). The official probe evaluates the teacher (--checkpoint_key teacher), so that -- not the student get_encoder() returns -- is the representation shipped.",
    "adapter/__main__.py": "new: python -m adapter entry point.",
    "configs/pretrain.yaml": "new: the pretrain recipe as a contract config, with data.val_path and training.optimizer dropped (the trainer does not read them) and the cluster output path removed.",
    "configs/linear_eval.yaml": "new: the linear-evaluation recipe as a contract config; the teacher backbone and online protocol are fixed by the adapter.",
    "requirements.txt": "rewritten: floors for exactly what the ported files import (torch, torchvision, numpy, Pillow, PyYAML, tensorboard). timm/tqdm/einops from the capture are not imported."
  }
}
