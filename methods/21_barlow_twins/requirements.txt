# What this method imports, and nothing else. Checked in both directions by
# tests/test_method_requirements.py.
#
# The captured file also listed timm, tqdm and numpy. **Step 1 imports none of
# them**, measured across the trainer, the model, the loader and the
# optimiser: timm and tqdm belong to the step 2 track that was not brought
# across, and numpy arrives as a dependency of torch rather than as something
# this code calls.
#
# **Pillow is here, and it was not in the port before this one.** This
# method's augmentation builds its own solarisation and blur on PIL rather
# than going through torchvision, so the import is real.
#
# tensorboard is here because the captured trainer imports SummaryWriter at
# module level.
#
# These are floors. Use requirements.lock.txt to rebuild a run.

torch>=2.1.0
torchvision>=0.16.0
Pillow>=9.5.0
PyYAML>=6.0
tensorboard>=2.14.0
