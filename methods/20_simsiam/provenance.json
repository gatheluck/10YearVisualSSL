{
  "captured_at": "2026-07-31",
  "captured_from": "gatheluck/10YearVisualSSLCapturePrivate, branch snapshots",
  "captured_path": "methods/20_simsiam",
  "captured_sha256": {
    "models/simsiam_resnet.py": "23f65603f3825b4348729d059fea63aa27bb7076717a3bc14978d62edc6e2362",
    "data/simsiam_dataset.py": "ec365daafc1d88eb55bad5d9c1b9d49b75d61f0d1eb7df393efd572ae0199c2a"
  },
  "note": "Only captured_sha256 came across untouched. Step 2 (train_step2_vit.py and models/simsiam_vit.py) was not brought across: it has no official-style variant in the capture, which is the same reason step 2 was left out of the first port. Both stages are ported: step 1 and the official-style linear evaluation. The checkpoint directory in the captured config was an absolute path on the cluster and is not shipped; the adapter puts it under --out.",
  "rewritten_during_the_port": {
    "__init__.py": "empty in the capture; kept empty",
    "models/__init__.py": "re-exported SimSiamViT (step 2), which was not brought across",
    "data/__init__.py": "same",
    "train_pretrain_resnet.py": "resolve_device() and make_deterministic() added, and the unconditional .cuda(local_rank) on the model and on each batch replaced by .to(device) -- the captured trainer could not start without a GPU. main() split into build_parser() and run(args, config), which now returns the epoch loss, the collapse monitor and the epoch count; the captured version computed them and discarded them.",
    "evaluate_linear_official.py": "captured sha256 a11f2ea65d71586105d4fd2eeac63ecda00ca191c2ce06fc242013765242cc1d. resolve_device() and make_deterministic() imported from the trainer -- one implementation per method -- and the unconditional torch.device('cuda:...') and torch.cuda.set_device replaced; the captured evaluation could not start without a GPU. main() split into build_parser() and run(args, encoder, in_dim), which now returns its metrics; the captured version wrote results.json and returned nothing. run() accepts an encoder built by the caller, because the contract's artifact is encoder.pt (the backbone alone) while the captured loader rebuilds the whole model from a training checkpoint with strict=True -- teaching this file a second way to recognise a file would be a second place that knows how an encoder is loaded. The model_type='vit' path now refuses by name: step 2 was not brought across, so its model is absent and the captured code failed as an ImportError three frames away."
  }
}
