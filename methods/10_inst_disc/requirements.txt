# What the 10_inst_disc port's own files import, and nothing else. Checked in both
# directions by tests/test_method_requirements.py. Floors only; the exact hashed
# closure is in requirements.lock.txt (CPU) and requirements.lock.cu130.txt.
#
# A self-contained re-implementation (the lab's own instance-discrimination code,
# no submodule): the ResNet-50 model, the NCE memory-bank loss, the dataset, the
# trainer and the probe are all here. torch is imported directly; numpy seeds the
# run; torchvision provides ResNet-50, the ImageFolder and the transforms; PyYAML
# is read to load a YAML config on the stand-alone path.
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
PyYAML>=6.0
