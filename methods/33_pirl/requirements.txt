# What the 33_pirl port's own files import, and nothing else. Checked in both
# directions by tests/test_method_requirements.py. Floors only; the exact hashed
# closure is in requirements.lock.txt (CPU) and requirements.lock.cu130.txt.
#
# A self-contained re-implementation (the lab's own PIRL code, no submodule): the
# ResNet-50 model with image + jigsaw projection heads, the memory-bank NCE loss,
# the jigsaw dataset, the trainer and the probe are all here. torch is imported
# directly; torchvision provides ResNet-50, the ImageFolder and transforms; Pillow
# (PIL.Image) is imported directly to load and convert images; numpy is used by
# the tests' fixtures; PyYAML is read to load a YAML config on the stand-alone
# path. timm supplies the ViT-B/16 for the unified Step-2 path (arch: vit),
# imported lazily so the native ResNet-50 path does not need it; TensorBoard and
# tqdm are dropped.
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
Pillow>=9.5.0
PyYAML>=6.0
timm>=0.9.0
