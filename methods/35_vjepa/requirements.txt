# What the 35_vjepa port's own files import, and nothing else. Checked in both
# directions by tests/test_method_requirements.py. Floors only; the exact hashed
# closure is in requirements.lock.txt (CPU) and requirements.lock.cu130.txt.
#
# A submodule-import port: the V-JEPA ViT + predictor + 3D mask collator are the
# official facebookresearch/jepa code under third_party/jepa, imported through
# PYTHONPATH and never copied or installed. This port's own files import only torch
# (the trainer, the probe), torchvision (the ImageFolder + transforms), numpy (the
# seeding) and PyYAML (the YAML config on the stand-alone path). The upstream
# src/app modules import only torch/torchvision (measured), so the closure stays
# torch-only. No timm; TensorBoard and tqdm are dropped, and the DDP paths are not
# ported (single-process).
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
PyYAML>=6.0
