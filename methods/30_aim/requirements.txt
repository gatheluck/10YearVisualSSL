# What the 30_aim port's own files import, and nothing else. Checked in both
# directions by tests/test_method_requirements.py. Floors only; the exact hashed
# closure is in requirements.lock.txt (CPU) and requirements.lock.cu130.txt.
#
# An eval-only port (linear_eval, no step 1). The AIM backbone is the pinned
# upstream under third_party/ml-aim (Apple's `aim` package), imported through
# PYTHONPATH and never copied or installed, so this port's own files import only
# torch (the probe), torchvision (the ImageFolder + val transforms), numpy (the
# seeding) and PyYAML (the YAML config on the stand-alone path). The ml-aim model
# module additionally imports huggingface_hub; that is the pinned upstream's own
# dependency, so it is in the lock (a closure) but not declared here. No timm.
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
PyYAML>=6.0
