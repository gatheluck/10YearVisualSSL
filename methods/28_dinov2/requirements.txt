# What the 28_dinov2 port's own files import, and nothing else. Checked in both
# directions by tests/test_method_requirements.py. Floors only; the exact hashed
# closure is in requirements.lock.txt (CPU) and requirements.lock.cu130.txt.
#
# An eval-only port (linear_eval, no step 1). The DINOv2 backbone is the pinned
# upstream under third_party/dinov2 (imported, not copied), built with the
# xformers path disabled so the forward is torch-only. torch is imported
# directly; numpy seeds the run; torchvision provides the ImageFolder and
# transforms; PyYAML is read to load a YAML config on the stand-alone path. The
# from-scratch SSL pretraining (LVD-142M) is the capture's excluded step; this
# probes a frozen, hash-pinned downloaded checkpoint. No timm, no tensorboard.
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
PyYAML>=6.0
