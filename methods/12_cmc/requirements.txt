# What the 12_cmc port's own files import, and nothing else. Checked in both
# directions by tests/test_method_requirements.py. Floors only; the exact hashed
# closure is in requirements.lock.txt (CPU) and requirements.lock.cu130.txt.
#
# A self-contained re-implementation (the lab's own CMC AlexNet code, no
# submodule): the two-branch encoder, the NCE memory bank, the alias sampler, the
# Lab dataset, the trainer and the probe are all here. torch is imported
# directly; numpy runs the CIE Lab conversion and seeds the run (scikit-image is
# reimplemented, not depended on); torchvision provides the ImageFolder and
# transforms; PyYAML is read to load a YAML config on the stand-alone path. timm
# supplies the two ViT-B/16 branches for the unified Step-2 path (arch: vit),
# imported lazily so the native AlexNet path does not need it.
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
PyYAML>=6.0
timm>=0.9.0
