# What the image_gpt port's own files import, and nothing else. Checked in both
# directions by tests/test_method_requirements.py. Floors only; the exact hashed
# closure is in requirements.lock.txt (CPU) and requirements.lock.cu130.txt.
#
# This is a self-contained re-implementation (no submodule): the model, the
# colour quantiser and the training loop are all here, so the closure is just
# this stack. torch is imported directly; numpy backs the deterministic k-means
# colour quantiser and the seeding; torchvision provides the ImageFolder and
# transforms the trainer and the probe read images with; PyYAML is read to load
# a YAML config on the stand-alone path.
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
PyYAML>=6.0
