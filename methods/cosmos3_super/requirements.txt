# What the cosmos3_super port's own files import, and nothing else. Checked in
# both directions by tests/test_method_requirements.py. Floors only; the exact
# hashed closure is in requirements.lock.txt (CPU) and requirements.lock.cu130.txt.
#
# There is NO author submodule: the Cosmos3-Super model class is transformers'
# Qwen3VLVisionModel (a pinned pip dependency), and the released weights are a
# sha256-pinned, public download recorded as backbone_artifact in provenance.json.
#
# torch is imported directly; numpy backs the seeding and feature handling;
# transformers provides Qwen3VLVisionModel and Qwen3VLVisionConfig; torchvision
# provides the ImageFolder and transforms the probe reads images with; PyYAML is
# read to load a YAML config on the stand-alone path. (safetensors is not imported
# here -- transformers reads model.safetensors internally; it appears only in the
# lock as transformers' own dependency.)
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.30.0
numpy>=1.24.0
PyYAML>=6.0
